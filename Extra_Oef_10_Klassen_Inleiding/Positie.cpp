#include "Positie.h"

Positie::Positie(const int x, const int y, const int z): x(x),
                                                         y(y),
                                                         z(z)
{
}
